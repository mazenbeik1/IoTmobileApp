import { Text } from "native-base";
import { ScrollView } from "native-base";

const Settings = () => {
    return ( 
        <ScrollView>
            <Text>Import settings here</Text>
        </ScrollView>
     );
}
 
export default Settings;