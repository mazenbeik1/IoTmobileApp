import { Text } from "native-base";
import { ScrollView } from "react-native";

const Details = () => {
    return ( 
        <ScrollView>
            <Text>Smart Home controller is a web app, whiich executes predefined functions that control a smart circuit. These predefined functions are developed according to user's devices and requirements.</Text>
        </ScrollView>
     );
}
 
export default Details;